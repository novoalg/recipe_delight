<div class="row">
    <div class="large-4 columns">
    <br/>
        <%= form_tag search_site_item_path do %>
            <h1>Search for products </h1>
            <%= text_field_tag "Search" %>
            <%= submit_tag "Search" %>
        <% end %>
    </div>
</div>
    <div class="row">
    <% @items.each do |item| %>
        <table>
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Price</th>
                    <th>Amount</th>
                </tr>
            <tbody>
                <tr>
                    <td><%= item.name %></td>
                    <td><%= item.price %></td>
                    <td><%= item.unit %></td>
                </tr>
            </tbody>
        </table>
    <% end %>
    </div>
<h1>Search for Target Products</h1>
<div class="large-5 columns">
<%= form_tag search_item_path do %>
    <%= text_field_tag "Search" %>
    <%= submit_tag "Search" %>
<% end %>
</div>
